const participate = ['boardId', 'title', 'boardCommentList', 'createdAt', 'nickname'];
const boardDetail = ['boardId', 'title', 'content', 'createdAt', 'nickname', 'boardTagList'];

export default { participate, boardDetail };
